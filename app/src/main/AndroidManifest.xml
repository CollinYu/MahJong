<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.up.yu18.mahjong">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat">
        <activity
            android:name="edu.up.yu18.mahjong.game.MahJong.game.MahJongMainActivity">
            <!--
            1/29/2017
            Tried to start project with no activity, and when we created our activity and later
            attempted to load it onto our tablet, it did not work. So we found this section of code
            at http://stackoverflow.com/questions/15825081/error-default-activity-not-found to fix
            the problem.
            -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
    </application>

</manifest>